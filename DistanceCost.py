import streamlit as st

def main():
    st.set_page_config(page_title="คำนวณค่าใช้จ่ายการเดินทาง", page_icon="🚗", layout="centered")

    st.markdown("""
        <h2 style='text-align:center; color:#4CAF50;'>🚗💡 เครื่องมือคำนวณค่าใช้จ่ายการเดินทาง</h2>
        <p style='text-align:center;'>เลือกรถที่ต้องการ ใช้งานง่าย ดูสบายตา ทันสมัย</p>
    """, unsafe_allow_html=True)

    st.write("---")

    # ค่าคงที่
    rate_fuel = 4
    rate_ev = 1

    st.subheader("📌 อัตราค่าใช้จ่ายต่อกิโลเมตร")
    col1, col2 = st.columns(2)
    with col1:
        st.info(f"รถยนต์ใช้น้ำมัน: **{rate_fuel} บาท/กม.**")
    with col2:
        st.success(f"รถไฟฟ้า: **{rate_ev} บาท/กม.**")

    st.write("---")

    # Input ระยะทาง
    distance = st.number_input("🚀 ระบุระยะทางที่ต้องการเดินทาง (กิโลเมตร)", min_value=0.0, format="%.2f")
    st.write(f"คุณต้องการเดินทาง: **{distance:.2f} กม.**")

    # เลือกรถ
    Vehicle_Type = st.radio(
        "🚘 เลือกประเภทยานพาหนะ",
        ("รถยนต์ใช้น้ำมัน", "รถไฟฟ้า"),
        horizontal=True
    )

    rate = rate_fuel if Vehicle_Type == "รถยนต์ใช้น้ำมัน" else rate_ev
    pay_total = distance * rate

    st.write("---")

    st.subheader("📊 สรุปผลการคำนวณค่าใช้จ่าย")
    st.markdown(f"""
        <div style='padding:15px; border-radius:10px; background:#F7F7F7;'>
            <p>🔎 <b>ระยะทางรวม:</b> {distance:.2f} กิโลเมตร</p>
            <p>🚘 <b>ยานพาหนะที่ใช้:</b> {Vehicle_Type}</p>
            <p>💰 <b>อัตราค่าใช้จ่าย:</b> {rate:.2f} บาท/กม.</p>
            <h3 style='color:#2E7D32;'>💵 ค่าใช้จ่ายรวม: {pay_total:.2f} บาท</h3>
        </div>
    """, unsafe_allow_html=True)

    st.write("\n")
    st.caption("สร้างด้วย Streamlit ✨ | ดูดี ใช้งานง่าย")


if __name__ == "__main__":
    main()
